{
  "name": "school-lunch-rating",
  "version": "1.0.0",
  "scripts": {
    "start": "node server/index.js",
    "heroku-postbuild": "pwd && ls -la && echo 'Obsah adresáře client:' && ls -la client"
  },
  "engines": {
    "node": "16.x"
  }
}
