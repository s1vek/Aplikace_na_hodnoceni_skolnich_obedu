{
  "name": "school-lunch-rating",
  "version": "1.0.0",
  "scripts": {
    "start": "node server/index.js",
    "heroku-postbuild": "pwd && ls && cd client && npm install && npm run build"
  },
  "engines": {
    "node": "16.x"
  }
}
