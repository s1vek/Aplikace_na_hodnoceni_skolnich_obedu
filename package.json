{
  "name": "school-lunch-rating",
  "version": "1.0.0",
  "scripts": {
    "start": "node server/index.js",
    "heroku-postbuild": "chmod +x heroku-build.sh && bash heroku-build.sh"
  },
  "engines": {
    "node": "16.x"
  }
}
